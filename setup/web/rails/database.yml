# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
# development:
  # adapter: postgresql
  # host: expiry_sync_postgres
  # username: expiry_sync
  # password: admin
  # database: expiry_sync
  # template: template0

development:
  adapter: mysql2
  host: db-server
  username: root
  password: password
  database: expiry_sync
  encoding: utf8

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: sqlite3
  database: db/test.sqlite3
  pool: 5
  timeout: 5000

# production:
#   adapter: postgresql
#   host: expiry_sync_postgres
#   username: expiry_sync
#   password: admin
#   database: expiry_sync
#   template: template0

production:
  adapter: mysql2
  host: db-server
  username: root
  password: password
  database: expiry_sync
  encoding: utf8